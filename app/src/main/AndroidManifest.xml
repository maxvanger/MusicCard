<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.fantom.vanger.musikcards">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- <activity android:name=".MainPager"> -->
        <!-- </activity> -->
         <activity android:name=".StartMenuActivity">
             <intent-filter>
                 <action android:name="android.intent.action.MAIN"/>
                 <category android:name="android.intent.category.LAUNCHER"/>
             </intent-filter>
         </activity>
        <activity android:name=".MusicCardsActivity">
         </activity>
        <activity android:name=".MusicPairsActivity">
        </activity>
        <activity android:name=".SettingsActivity">
        </activity>
        <provider android:name=".ProviderPuzzle"
                  android:authorities="com.fantom.vanger.musikcards.ProviderPuzzle"/>
    </application>

</manifest>